# Numpad 整板 PCB 设计文档

> 目标：把 nice!nano v2 模块 + numpad shield 的全部功能集成到单块 PCB，nRF52840 直接上板，不再使用 Pro Micro 模块。
>
> **当前方案**：沿用 nice!nano 开发板（不画整板）。本文档为整板设计参考，转为备用，将来紧凑化时启用。
>
> **第一参考**：`nicenano_NRF52840资料/原理图/nice！nano_v2-原理图_1.0.pdf` —— 最小系统与电源直接照抄。
> **芯片手册**：`nicenano_NRF52840资料/52840官方介绍/nRF52840_PS_v1.1.pdf`

## 1. 功能范围（默认全保留）

| 功能 | 说明 |
|------|------|
| 蓝牙 BLE | nRF52840 自带，2.4GHz 板载/陶瓷天线 |
| USB-C | 供电 + HID + DFU 刷写 |
| 锂电池充电 | 单节 LiPo，USB 时充电、拔线后蓝牙工作 |
| 5×4 矩阵 | 编码器按压复用 R4C0 空位 |
| EC11 编码器 | A/B + 按下 |
| AI32C 触摸滑块 | 3 通道电容触摸，2 线编码输出（替换 OLED）|
| WS2812 RGB | 每键 1 颗共 19 颗（SPIM3）+ 2 颗电量灯（SPIM2）|
| 电源开关 | 机械拨动开关控制电池通断 |
| 拨码开关（可选）| 2 位，机械切 AI32C / RGB 外设电源，4 档省电 |

## 2. 系统框图

```
                ┌───────── USB-C ─────────┐
                │   5V  ──┬── 充电IC ── 电池 ──┐
                │         │                   │
                │   D+/D- │                   │ 电源开关
                │         │                   │
                │         └── VBUS检测 ──┐    │
                │                          │  │
                ▼                          ▼  ▼
        ┌──────────────────────── nRF52840 ────────────────────────┐
        │  VDDH(高压域) ◄── 电池/USB 5V                              │
        │     │ 内部 DCDC + 外部电感                                 │
        │     ▼                                                     │
        │  VDD(1.7~3.6V) ── 系统 3.3V                              │
        │                                                           │
        │  GPIO 行/列 ── 矩阵   GPIO ── AI32C 触摸   QDEC ── EC11  │
        │  SPIM3 ── WS2812×19                                     │
        │  USB D+/D-        ANT ── PI 匹配 ── 50Ω ── 天线        │
        │  SWD 调试口       RESET 键                             │
        └───────────────────────────────────────────────────────────┘
```

## 3. 模块设计

### 3.1 nRF52840 最小系统（最易翻车，照抄 nice!nano）

- **封装**：nRF52840 QFAA（QFN-100，7×7mm，0.4mm 间距），建议贴片，手焊极难
- **HFXO（高频晶振）**：32MHz + 2×12pF 负载电容，走线短、包地，蓝牙必须
- **LFXO（低频晶振）**：32.768kHz + 2×12pF，低功耗/RTC 唤醒必须
- **DCDC 电感**：约 10µH，接 DCC↔DEC4，启用内部 DCDC 降功耗（整板靠它省电）
- **去耦**：每个 VDD/VDDH 引脚就近 100nF，VDDH 加 4.7µF
- **射频前端**：ANT → PI 匹配网络（参考 Nordic DK 原理图，约 2 电感+1 电容）→ 50Ω 走线 → 板载 PCB 天线或陶瓷天线
- **复位**：RESET 引脚 + 10k 上拉 + 复位按键（双击进 DFU）
- **SWD 调试口**：SWDIO / SWCLK / RESET / GND / VTREF 引出测试点 —— **首次烧 bootloader 必须**，没有它板子无法启动

> 射频匹配、晶振负载电容的最终值以 nRF52840 PS 和 nice!nano v2 原理图为准。

### 3.2 电源

供电树（参考 nice!nano v2）：

```
USB-C 5V ─┬─> 充电 IC ─> 电池(3.0~4.2V) ─┬─> 电源开关 ─> VDDH
          │                              │
          └─> VBUS 检测(分压) ─> GPIO     └─> nRF52840 内部 DCDC ─> VDD(3.0V) ─> 系统 3.3V
```

- **充电 IC**：MCP73831 / BQ21040 / TP4054，单节锂电，500mA 左右
- **电池→系统**：nRF52840 的 VDDH 支持 2.1–5.5V，电池直接进 VDDH，内部 DCDC 降到 VDD，**无需外部 LDO**（nice!nano 做法）
- **VBUS 检测**：USB 5V 经分压（如 2:1）到 GPIO，用于检测插拔、充电唤醒
- **电源开关**：串在电池正极与 VDDH 之间
- **电池保护**（可选）：DW01 + FS8205A 过充过放保护

### 3.3 USB-C
- 接口：USB-C 16pin 或 24pin，5V（VBUS）+ D+/D- + GND
- D+/D- 直接连 nRF52840（内置 USB PHY，无需外部芯片）
- CC1/CC2 各串 5.1k 下拉到 GND（作 device）
- VBUS 加 ESD 保护（可选）

### 3.4 键盘矩阵
- 现有 6 行 × 4 列，直接 GPIO
- 每键串 1N4148（或 BAT54）防鬼影，二极管方向匹配 ZMK（现有 `col2row`：阳极接列、阴极接行，电流列->行）
- 整板不再受 Pro Micro 排针限制，引脚可按走线重排（见 §4）

标准 numpad 物理布局（PCB 排键参考）：

```
R0:  [ Num ][  /  ][  *  ][  -  ]
R1:  [  7  ][  8  ][  9  ][     ]
                            [  +  ]  ← + 2U 高（跨 R1-R2，轴在 R2C3）
R2:  [  4  ][  5  ][  6  ][     ]
R3:  [  1  ][  2  ][  3  ][     ]
                            [ Ent ]  ← Enter 2U 高（跨 R3-R4，轴在 R4C3）
R4:  [   0 (2U 宽)   ][  .  ][   ]
       ↑ 0 跨 C0-C1，轴在 R4C1，R4C0 复用为编码器按压
```

- 2U 大键：+ 和 Enter 各 2U 高，0 为 2U 宽（均单轴，覆盖的另一个矩阵位置为空位）
- 已删 FN 键，编码器按下接管切层（音量/亮度），不再占独立键位
- 编码器旋钮物理位置由 PCB 决定，按下复用矩阵 R4C0（0 键 2U 宽的空位，行 D18 + 列 D2），无需独立虚拟行

### 3.5 编码器 EC11
- A/B 相各加 RC 去抖（100Ω + 0.1µF，或 0.1µF 到 GND）
- 公共脚接 GND
- 按下复用矩阵 R4C0（行 D18 + 列 D2），和其他键一样串二极管（col2row：阳极接列、阴极接行）

### 3.6 AI32C 触摸滑块
- 3 通道电容触摸，SOP-8，2.5-5.5V 供电（整板用 3.3V）
- 输出 2 线编码（OUT1+OUT2），由 ZMK 自定义 kscan driver 解码
- 触摸感应盘：PCB 铜箔，3mm×3mm~30mm×30mm，相邻盘间距 >4mm
- 感应盘底层不铺地，走线尽量短细（<100mm，避开通讯线）
- 外围：C1=4.7nF NPO、VDD 串 20Ω（不可省）、100nF 退耦、OUT1/OUT2 各 1K 上拉

### 3.7 WS2812 RGB 灯珠
- 每键 1 颗共 19 颗，SPIM3 MOSI（D20 / P0.29）驱动，4MHz
- **供电**：从 VDDH/RAW 取电（勿用 3.3V），每颗全白约 60mA，19 颗 ≈ 1.2A
- 每 5-10 颗加 100nF 去耦电容，靠近灯珠
- DATA 走线尽量短，远离电源/干扰线；软件限制最大亮度保护电池

### 3.8 拨码开关省电控制（可选）
- 2 位机械拨码，切外设电源：`3.3V─位1─20Ω─AI32C VDD`、`RAW─位2─WS2812 主链 VDD`
- **电量链（2 颗）独立供电**（RAW 直连，不经位2）——省电关灯时仍显示电量/充电
- 4 档：00 关 RGB+触摸（电量链在）/ 01 仅触摸 / 10 仅 RGB / 11 全功能
- 位2 须过 1.2A（选 ≥1.5A 拨码）；位1 关触摸后触摸唤醒失效

### 3.9 Bootloader & SWD
- nRF52840 空片无 bootloader，**首次必须 J-Link 烧** `nicenano_NRF52840资料/BootLoader/nice_nano/dx_nice_nano_nrf52840_bootloader_s140_6.1.1.hex`
- 烧完后才能 USB 拖拽 uf2
- 工具：J-Link + nRF Connect Programmer（资料 `软件工具/` 内）

### 3.10 LED 指示（可选）
- 充电状态 LED（红/蓝，充电 IC 输出驱动）
- 蓝牙/系统 LED（GPIO 驱动）

## 4. 引脚分配（沿用 PINOUT.md，可直接用或按布线重排）

Zephyr 节点形式：`&gpio0 N` = P0.N，`&gpio1 N` = P1.N。

| 功能 | Pro Micro | nRF52840 | Zephyr 节点 |
|------|-----------|----------|-------------|
| 行 R0 | D10 | P0.9 | `&gpio0 9` |
| 行 R1 | D16 | P0.10 | `&gpio0 10` |
| 行 R2 | D14 | P1.11 | `&gpio1 11` |
| 行 R3 | D15 | P1.13 | `&gpio1 13` |
| 行 R4 | D18 | P1.15 | `&gpio1 15` | 含编码器按压（R4C0）|
| 列 C0 | D2 | P0.17 | `&gpio0 17` |
| 列 C1 | D3 | P0.20 | `&gpio0 20` |
| 列 C2 | D4 | P0.22 | `&gpio0 22` |
| 列 C3 | D5 | P0.24 | `&gpio0 24` |
| EC11 A | D8 | P1.4 | `&gpio1 4` |
| EC11 B | D9 | P1.6 | `&gpio1 6` |
| AI32C OUT1 | D6 | P1.0 | `&gpio1 0` |
| AI32C OUT2 | D7 | P0.11 | `&gpio0 11` |
| AI32C VDD | 3.3V | — | 串 20Ω（不可省）|
| AI32C C1 | — | — | 4.7nF NPO → GND |
| WS2812 DATA | D20 | P0.29 | `&gpio0 29` | SPIM3 MOSI，4MHz，19 颗 RGB |
| WS2812 电量 | D19 | P0.2 | `&gpio0 2` | SPIM2 MOSI，4MHz，2 颗电量灯 |
| VBUS 检测（新增）| — | 待分配 | 任一空闲 GPIO |
| UART（调试，可选）| D0/D1 | P0.8/P0.6 | `&gpio0 8`/`&gpio0 6` |
| 预留扩展 | D21 | P0.31 | `&gpio0 31` |

> 整板可重新分配引脚以优化走线，软件侧同步改 overlay 即可。

## 5. PCB 布局规则

- **MCU 居中**，射频区/天线放板边，**远离金属**（轴体、电池、螺丝都会挡信号）
- 32MHz 晶振紧贴 XC1/XC2，下方完整地平面
- RF 走线尽量短，控 50Ω 阻抗，下方不跨分割
- 去耦电容贴 MCU 同侧、就近引脚
- 充电 IC 靠近 USB-C 和电池接口
- 建议 **4 层板**（信号-地-电源-信号），射频/电源更稳；2 层省钱但走线要小心
- 关键地：晶振、射频、USB 各自就近回流

## 6. BOM 草案（关键件）

| 类别 | 型号 | 备注 |
|------|------|------|
| MCU | nRF52840 QFAA | 100pin QFN |
| 充电 IC | MCP73831 / BQ21040 | 单节锂电 |
| 电感（DCDC）| 10µH | nRF 内部 DCDC |
| 电感（RF 匹配）| 参考 DK | 2 个 |
| 晶振 | 32MHz + 32.768kHz | 各 1 |
| 天线 | 板载 PCB 或陶瓷 | 2.4GHz |
| 二极管 | 1N4148 / BAT54 | 每键 1 颗 |
| 编码器 | EC11 | 含按压 |
| 触摸芯片 | AI32C | SOP-8，3 通道电容触摸 |
| RGB 灯珠 | WS2812 | 每键 1 颗，19 颗 |
| 接口 | USB-C | 16/24pin |
| 保护 | DW01 + FS8205A（可选）| 电池保护 |

## 7. 固件适配（配套工作）

整板化后 overlay 需从 Pro Micro 抽象解耦，两条路线：

- **方案 A（最小改动，推荐先做）**：仍搭 `nice_nano` board（nRF52840 SoC 配置整板通用），把 overlay 里 `&pro_micro N` 换成 `&gpio0/&gpio1 N` 直连，AI32C 触摸已用自定义 kscan driver（`extra-module/drivers/kscan/kscan_gpio_ai32c.c`），去掉 `requires: [pro_micro]`。构建命令不变。用于先验证自定义引脚可编译。
- **方案 B（原生整板）**：自建 ZMK board（`boards/arm/<板名>/`），矩阵/编码器/AI32C 写进 board，构建变 `-b <板名>`，不再需要 shield。改动大但干净，等 PCB 定型后再做。

## 8. 推进路线

1. ✅ 功能范围确认（默认全保留）
2. 原理图：先抄 nice!nano 最小系统 + 电源，再加矩阵/编码器/AI32C/USB
3. ERC 检查
4. PCB 布局布线（4 层优先）
5. 打样小批量
6. J-Link 烧 bootloader → USB 烧固件验证
7. 固件：先方案 A 验证，PCB 定型后升级方案 B
```
